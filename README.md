# Обновляем README.md
PS C:\Users\Rodion\IdeaProjects\ibank-test> @"
# Автотесты для интернет-банка

[![Java CI with Gradle](https://github.com/ваш-username/ibank-test/actions/workflows/appveyor.yml/badge.svg)](https://github.com/ваш-username/ibank-test/actions/workflows/appveyor.yml)

## Описание
Автоматизированные тесты для проверки функциональности авторизации в интернет-банке с использованием тестового режима.

## 🚀 Технологии
- **Java 11** - основной язык программирования
- **JUnit 5** - фреймворк для тестирования
- **Selenide** - автоматизация браузера
- **REST Assured** - работа с REST API
- **Lombok** - сокращение boilerplate кода
- **Faker** - генерация тестовых данных
- **Gradle** - система сборки

## 📋 Тестовое покрытие

### ✅ Позитивные сценарии:
- Успешная авторизация активного пользователя

### ❌ Негативные сценарии:
- Блокировка входа для заблокированного пользователя
- Ошибка авторизации при неверном логине
- Ошибка авторизации при неверном пароле
- Валидация формы при пустом логине
- Валидация формы при пустом пароле
- Перезапись данных пользователя

## 🛠 Установка и запуск

### Предварительные требования:
- Java 11 или выше
- Gradle 6.0 или выше

### Локальный запуск:

1. **Клонируйте репозиторий:**
   \`\`\`bash
   git clone https://github.com/ваш-username/ibank-test.git
   cd ibank-test
   \`\`\`

2. **Запустите приложение в тестовом режиме:**
   \`\`\`bash
   java -jar artifacts/app-ibank.jar -P:profile=test
   \`\`\`

3. **Запустите тесты (в отдельном терминале):**
   \`\`\`bash
   ./gradlew test
   \`\`\`

### Запуск в CI:
Тесты автоматически запускаются при пуше в main ветку через GitHub Actions.

## 📁 Структура проекта

\`\`\`
ibank-test/
├── src/test/java/ru/netology/ibank/
│   ├── data/
│   │   ├── AuthInfo.java          # Данные для авторизации
│   │   ├── RegistrationDto.java   # Данные для регистрации
│   │   └── DataGenerator.java     # Генератор тестовых данных
│   └── AuthTest.java              # Основные тесты
├── artifacts/
│   └── app-ibank.jar              # Тестируемое приложение
├── .github/workflows/
│   └── appveyor.yml               # CI конфигурация
├── build.gradle                   # Конфигурация сборки
└── README.md                      # Документация
\`\`\`

## 🧪 Особенности тестирования

### Тестовый режим:
Для тестирования используется специальный режим приложения с доступом к API создания пользователей:
\`\`\`bash
java -jar app-ibank.jar -P:profile=test
\`\`\`

### API для подготовки данных:
\`\`\`http
POST /api/system/users
Content-Type: application/json

{
\"login\": \"vasya\",
\"password\": \"password\",
\"status\": \"active\"
}
\`\`\`

## 📊 Результаты тестирования

**Статус:** ✅ Все тесты проходят успешно

**Последний запуск:** $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")

**Покрытие:** 100% основных сценариев авторизации

## 🤝 Contributing
1. Форкните репозиторий
2. Создайте feature ветку (\`git checkout -b feature/amazing-feature\`)
3. Закоммитьте изменения (\`git commit -m 'Add amazing feature'\`)
4. Запушьте в ветку (\`git push origin feature/amazing-feature\`)
5. Откройте Pull Request

## 📄 Лицензия
Этот проект создан в учебных целях в рамках курса Netology.

---
*Время выполнения: [см. testing-time.md](testing-time.md)*
"@ | Out-File -FilePath "README.md" -Encoding UTF8